using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.InputSystem;

public class MouseBehaviour : MonoBehaviour
{
    // under construction

    /*private bool isBartyActive;
    private float speed = 5f;
    private Vector2 currentPosition = Mouse.current.position;

    private void WarpingCursor()
    {
        if (isBartyActive)
        {
            currentPosition += Vector2.left * speed * Time.deltaTime;
            Mouse.current.WarpCursorPosition(currentPosition);
        }
    }*/
}
